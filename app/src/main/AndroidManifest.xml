<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools"
    package="com.salwa.androidelearning">

    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.WRITE_EXTERNAL_STORAGE" />
    <uses-permission android:name="android.permission.READ_EXTERNAL_STORAGE" />

    <application
        android:allowBackup="true"
        android:appComponentFactory="whateverString"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme"
        tools:replace="android:appComponentFactory">
        <activity android:name=".activity.StudentMainActivity" />
        <activity android:name=".activity.LoginActivity" />
        <activity android:name=".activity.RegisterationActivity" />
        <activity android:name=".activity.ForgotPassword" />
        <activity android:name=".StudentActivity" />
        <activity android:name=".TeacherActivity" />
        <activity android:name=".activity.TeacherMainActivity" />
        <activity android:name=".activity.SplashScreen">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".activity.TeacherOrStudentActivity" />
        <activity android:name=".activity.CreationActivity" />
        <activity android:name=".activity.LessonCreationActivity" />

        <!-- <provider -->
        <!-- android:name="android.support.v4.content.FileProvider" -->
        <!-- android:authorities="${applicationId}.provider" -->
        <!-- android:exported="false" -->
        <!-- android:grantUriPermissions="true"> -->
        <!-- <meta-data -->
        <!-- android:name="android.support.FILE_PROVIDER_PATHS" -->
        <!-- android:resource="@xml/provider_paths"/> -->
        <!-- </provider> -->
        <activity android:name=".activity.LessonsAndActivitiesActivity" />
        <activity android:name=".activity.QuizzesActivity" />
        <activity
            android:name=".activity.VideoPreview"
            android:configChanges="orientation"
            android:screenOrientation="portrait" />
        <activity android:name=".activity.ProgressActivity" />
        <activity android:name=".activity.QuizzesCreation" />
        <activity android:name=".activity.QuizActivity" />
        <activity android:name=".activity.StudentProfile" />
        <activity android:name=".activity.PreCreationActivity"></activity>
    </application>

</manifest>